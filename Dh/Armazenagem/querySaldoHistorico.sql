SELECT IFNULL(SUM(CASE WHEN ATUALESTOQUE = 1 THEN PESO_ITEM WHEN ATUALESTOQUE = -1 THEN -PESO_ITEM ELSE 0 END), 0) AS SALDO_HISTORICO FROM IMP_NOTAS_ARMAZ WHERE CODPARC = :VAR_PARCEIRO_SELECIONADO AND DTNEG <= LAST_DAY(CONCAT(:VAR_MES_SELECIONADO, '-01'))